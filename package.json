{
  "name": "chimpanzetype",
  "version": "1.0.0",
  "description": "A typing speed test application with user authentication",
  "main": "server/auth-server.js",
  "scripts": {
    "start": "node server/auth-server.js",
    "dev": "concurrently \"nodemon server/auth-server.js\" \"nodemon server/CreateAccount.js\"",
    "server:auth": "nodemon server/auth-server.js",
    "server:register": "nodemon server/CreateAccount.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["typing", "speed", "test", "game", "authentication"],
  "author": "ChimpanzeType Team",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "db-migrate-pg": "^1.3.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-notifier": "^10.0.1",
    "nodemon": "^3.0.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
